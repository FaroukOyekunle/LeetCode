✅ Explanation

- GROUP BY email: Groups all rows by the email address.

-   COUNT(*) > 1: Filters only those groups where the email appears more than once.

-   SELECT email AS Email: Outputs the duplicate emails with the required column name format.